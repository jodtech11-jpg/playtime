
// Firebase is currently disabled for this application.
// All authentication and data persistence are handled via Local Storage for this demo.

export const auth = null as any;
export const db = null as any;
export const storage = null as any;

export default null;
