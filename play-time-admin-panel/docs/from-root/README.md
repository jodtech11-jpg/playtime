# Moved from project root

These files were in the project root and have been moved here to keep the root clean.

- **Canonical docs** live in `docs/guides/`, `docs/implementations/`, `docs/planning/`, and `docs/firebase/`.
- These copies are kept for reference; prefer the versions in those folders.
