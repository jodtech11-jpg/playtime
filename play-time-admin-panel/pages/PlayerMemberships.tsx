import React from 'react';
import Memberships from './Memberships';

/** Super-admin page for Play Time Pro (player) membership management. */
const PlayerMemberships: React.FC = () => {
  return <Memberships mode="platform" />;
};

export default PlayerMemberships;
